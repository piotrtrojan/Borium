import { EpochDashboardDto } from './epoch-dashboard-dto';

describe('EpochDashboardDto', () => {
  it('should create an instance', () => {
    expect(new EpochDashboardDto()).toBeTruthy();
  });
});
