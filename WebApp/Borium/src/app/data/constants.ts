const apiEndpoint = 'http://localhost:55484/';
const apiPrefix = 'api/v1/';

const Constants = {
    Endpoints: {
        Epoch: {
            GetDashboard: apiEndpoint + apiPrefix + 'epoch/'
        }
    }
}
